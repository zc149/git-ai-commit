# v1.5.0: 버전 기능 및 빌드 시스템 개선

## 버전 정보
- **버전**: 1.5.0
- **날짜**: 2026-02-11
- **목표**: 버전 관리 기능 도입 및 빌드 시스템 개선

## 개요

v1.5.0에서는 버전 관리 기능, 빌드 시스템 개선, 그리고 성능 및 정확성 개선을 진행했습니다. 사용자가 `git-ai-commit --version` 명령으로 현재 버전을 확인할 수 있고, 빌드 시스템이 개선되어 릴리스 프로세스가 자동화되었습니다. 또한 대규모 커밋 처리 성능이 대폭 향상되었고, 커밋 타입 분류 및 scope 추천 정확도가 개선되었습니다.

## 동기

### 문제 1: 버전 정보 확인 불가
- 사용자가 설치된 버전을 알 수 없음
- 버전별 기능 차이 확인 어려움
- 버그 리포트 시 버전 정보 부족

### 문제 2: 수동 버전 관리
- 버전 업데이트 시 여러 파일을 수동으로 수정해야 함
- `main.go`, `go.mod` 등 파일 간 버전 불일치 가능성
- 릴리스 프로세스 수동화로 인한 실수 위험

### 문제 3: 대규모 커밋 처리 성능 저하
- 병렬 처리 임계값이 너무 높음 (5개 파일)
- 의존성 파일 감지가 O(n) 복잡도로 느림
- 파일 타입 분류도 O(n) 복잡도로 느림
- 100+ 파일 커밋에서 상당한 지연 발생

### 문제 4: 커밋 타입 분류 정확도 부족
- 기능 추가를 `build`로 잘못 분류
- 의존성 파일이 있으면 설정/빌드로 분류하는 경향
- 새 소스 파일 추가임에도 feat가 아닌 경우 발생

### 문제 5: Scope 추천 과도한 파일 나열
- 설정 파일만 있을 때 모든 파일 이름을 나열
- 같은 폴더에 없으면 `build(app1, app2, app3...)` 처럼 긴 scope
- 사용자가 직접 수정해야 하는 불편함

## 해결 방안

### 성능 개선

### 4. 병렬 처리 임계값 조절

**파일**: `internal/worker/pool.go`

**변경**: `GetOptimalWorkerCount()` 함수

```go
// 변경 전
const parallelThreshold = 5

// 변경 후
const parallelThreshold = 3
```

**효과**:
- 중간 규모 커밋(4-20 파일)에서도 병렬 처리 활용
- 15-30% 성능 향상 예상

### 5. 의존성 파일 감지 최적화

**파일**: `internal/git/diff.go`

**변경 전** (O(n) 복잡도):
```go
func isDependencyFile(path string) bool {
    base := filepath.Base(path)
    return base == "package.json" ||
           base == "package-lock.json" ||
           base == "yarn.lock" ||
           // ... 40개 이상의 if문
}
```

**변경 후** (O(1) 복잡도):
```go
var (
    dependencyFiles map[string]bool
    depFileOnce     sync.Once
)

func init() {
    depFileOnce.Do(func() {
        dependencyFiles = make(map[string]bool)
        dependencyFiles["package.json"] = true
        dependencyFiles["package-lock.json"] = true
        dependencyFiles["yarn.lock"] = true
        // ... 모든 의존성 파일
    })
}

func isDependencyFile(path string) bool {
    base := filepath.Base(path)
    return dependencyFiles[base]
}
```

**효과**:
- 최악의 경우 40배 이상 빨라짐
- 100파일 기준: 4000번 비교 → 100번 비교

### 6. 파일 타입 분류 최적화

**파일**: `internal/worker/pool.go`

**변경 전** (O(n) 복잡도):
```go
func classifyFileType(path string) FileType {
    ext := filepath.Ext(path)
    base := filepath.Base(path)
    
    if strings.HasSuffix(base, "_test.go") || /* ... */ {
        return FileTypeTest
    }
    if base == "README.md" || /* ... */ {
        return FileTypeDoc
    }
    if ext == ".yml" || ext == ".yaml" || /* ... */ {
        return FileTypeConfig
    }
    return FileTypeSource
}
```

**변경 후** (O(1) 복잡도):
```go
var (
    testFiles map[string]bool
    docFiles  map[string]bool
    configExts map[string]bool
    fileInitOnce sync.Once
)

func init() {
    fileInitOnce.Do(func() {
        testFiles = make(map[string]bool)
        testFiles["_test.go"] = true
        // ... 다른 테스트 파일 패턴
        
        docFiles = make(map[string]bool)
        docFiles["README.md"] = true
        // ... 다른 문서 파일
        
        configExts = make(map[string]bool)
        configExts[".yml"] = true
        configExts[".yaml"] = true
        // ... 다른 설정 파일 확장자
    })
}

func classifyFileType(path string) FileType {
    base := filepath.Base(path)
    
    if testFiles[base] {
        return FileTypeTest
    }
    if docFiles[base] {
        return FileTypeDoc
    }
    
    ext := filepath.Ext(path)
    if configExts[ext] {
        return FileTypeConfig
    }
    
    return FileTypeSource
}
```

**효과**:
- 파일 타입 분류 속도 대폭 향상
- 병렬 처리 시 전체 성능에 기여

### 정확성 개선

### 7. 커밋 타입 분류 로직 개선

**파일**: `internal/git/diff.go`

**변경**: `InferCommitType()` 함수

```go
// 새 소스 파일이 있으면 무조건 feat로 분류
if newSourceFileCount > 0 {
    typeScore["feat"] += 50      // 강력한 가중치
    typeScore["build"] -= 20      // 의존성 점수 감소
    typeScore["chore"] -= 20      // 설정 점수 감소
}

// 의존성 파일만 있는 경우에만 build로 분류
if hasDependencyFile && sourceFileCount == 0 && !hasRegularConfig {
    typeScore["build"] += 15
}

case FileTypeDoc:
    typeScore["docs"] += 3

case FileTypeSource:
    if file.IsNew {
        // 새 소스 파일 추가는 feat에 매우 강력한 점수
        typeScore["feat"] += 15
    } else if !file.IsDeleted {
        // 기존 소스 파일 수정 - refactor에 더 높은 가중치 부여
        typeScore["refactor"] += 10    // 5점 → 10점으로 증가
        typeScore["fix"] += 5         // 3점 → 5점으로 증가
    }
```

**개선 사항**:
1. 새 소스 파일 추가 시 `feat` 가중치 +50점
2. 의존성/설정 변경 점수 낮춤 (소스 파일 우선)
3. 새 소스 파일 임계값 3개 → 2개로 낮춤
4. 새 디렉토리 가중치 증가 (+20 → +30점)
5. **문서 파일 점수 낮춤**: 8점 → 3점
   - 문서 업데이트는 보통 소스 코드 변경에 수반되는 secondary 작업
   - 소스 코드와 문서가 함께 변경될 때 소스 코드 변경이 우선되도록 함
6. **리팩토링 점수 증가**: 5점 → 10점
   - 기존 소스 파일 수정의 중요성 반영
   - `fix` 점수도 3점 → 5점으로 증가

**결과 예시**:
- `feat: add api-proxy-backed plugin` → 올바르게 `feat`로 분류
- 소스 파일 수정 + 문서 업데이트 → `refactor`로 올바르게 분류 (기존: `docs`)

### 8. Scope 추론 로직 개선

**파일**: `internal/git/diff.go`

**변경**: `InferScopes()` 함수 리팩토링

```go
func InferScopes(files []FileChange) []string {
    // 1. 파일 유형별 분류
    var sourceFiles []FileChange
    var configFiles []FileChange
    var dependencyFiles []FileChange
    
    for _, file := range files {
        switch file.FileType {
        case FileTypeSource:
            sourceFiles = append(sourceFiles, file)
        case FileTypeConfig:
            if isDependencyFile(file.Path) {
                dependencyFiles = append(dependencyFiles, file)
            } else {
                configFiles = append(configFiles, file)
            }
        }
    }
    
    // 2. 소스 파일이 있으면 소스 파일 기반으로만 scope 결정
    if len(sourceFiles) > 0 {
        return inferScopeFromSourceFiles(sourceFiles)
    }
    
    // 3. 설정 파일만 있는 경우
    if len(dependencyFiles) > 0 {
        return inferScopeFromConfigFiles(append(dependencyFiles, configFiles...))
    }
    if len(configFiles) > 0 {
        return inferScopeFromConfigFiles(configFiles)
    }
    
    return []string{}
}

func inferScopeFromConfigFiles(configFiles []FileChange) []string {
    // 최상위 디렉토리 확인
    topLevelDirs := make(map[string]bool)
    for _, file := range configFiles {
        parts := strings.Split(filepath.Clean(file.Path), string(filepath.Separator))
        if len(parts) > 0 {
            topLevelDirs[parts[0]] = true
        }
    }
    
    // 하나의 디렉토리만 있으면 그걸 사용
    if len(topLevelDirs) == 1 {
        for dir := range topLevelDirs {
            return []string{simplifyScopeName(dir)}
        }
    }
    
    // 여러 디렉토리에 있으면 "config" 사용
    return []string{"config"}
}
```

**결과 예시**:
- 기존: `build(app-config.local.yaml, app-config.yaml, packages, plugins, yarn.lock)`
- 개선: `feat(config)` 또는 `feat(app-config)`

## 기존 기능

### 1. 버전 모듈 도입

**파일**: `internal/version/version.go`

```go
package version

const (
    Version = "1.5.0"
    BuildDate = "2026-02-11"
)
```

**특징**:
- 버전 정보를 별도 패키지로 분리
- 다른 패키지에서 쉽게 import하여 사용
- 빌드 시점에 버전 정보 주입 가능

### 2. CLI 버전 플래그 추가

**파일**: `cmd/root.go`

```go
import "git-ai-commit/internal/version"

func Execute() {
    rootCmd := &cobra.Command{
        Use:   "git-ai-commit",
        Short: "AI 기반 커밋 메시지 생성 도구",
        Run: func(cmd *cobra.Command, args []string) {
            // 기본 동작
        },
    }

    // 버전 플래그
    rootCmd.Flags().Bool("version", false, "버전 정보 표시")
    
    // 버전 플래그 처리
    if versionFlag, _ := cmd.Flags().GetBool("version"); versionFlag {
        fmt.Printf("git-ai-commit version %s (built %s)\n", 
            version.Version, version.BuildDate)
        return
    }
}
```

### 3. 빌드 스크립트 개선

**파일**: `build.sh`

```bash
#!/bin/bash

VERSION=${VERSION:-$(grep "const Version" internal/version/version.go | awk '{print $3}' | tr -d '"')}
BUILD_DATE=$(date +%Y-%m-%d)

echo "Building git-ai-commit v${VERSION}..."

# 버전 정보 주입
go build -ldflags="-X 'git-ai-commit/internal/version.Version=${VERSION}' -X 'git-ai-commit/internal/version.BuildDate=${BUILD_DATE}'" -o git-ai-commit main.go

echo "Build complete: git-ai-commit v${VERSION}"
```

**특징**:
- `go build`의 `-ldflags` 옵션으로 컴파일 시점에 버전 정보 주입
- `VERSION` 환경변수로 버전 지정 가능
- 빌드 날짜 자동으로 설정

## 사용 예시

### 버전 확인

```bash
$ git-ai-commit --version
git-ai-commit version 1.5.0 (built 2026-02-11)
```

### 버전 지정 빌드

```bash
# 개발 버전 빌드
VERSION=1.6.0-dev ./build.sh

# 릴리스 버전 빌드
VERSION=1.5.0 ./build.sh
```

## 구현 상세

### 1. 버전 모듈 설계

**이유**: 버전 정보 중앙화

**장점**:
- 한 곳에서 버전 관리
- 다른 패키지에서 쉽게 참조
- 테스트 코드에서도 사용 가능

**구조**:
```
internal/
├── version/
│   └── version.go    # 버전 정보 정의
├── git/
│   └── diff.go       # (버전 정보 사용 예시)
└── llm/
    └── groq.go       # (버전 정보 사용 예시)
```

### 2. 빌드 시점 버전 주입

**기술**: Go Linker Flags

**옵션**:
```bash
-ldflags="-X 'package_path.variable_name=value'"
```

**예시**:
```bash
go build -ldflags="-X 'git-ai-commit/internal/version.Version=1.5.0'"
```

**작동 원리**:
1. 컴파일 시점에 지정된 값으로 변수 초기화
2. `version.go`의 기본값이 아닌 주입된 값 사용
3. 바이너리에 버전 정보가 하드코딩됨

### 3. Cobra CLI 통합

**버전 플래그 처리**:
```go
// PreRun 훅에서 처리
rootCmd.PreRun = func(cmd *cobra.Command, args []string) {
    if showVersion, _ := cmd.Flags().GetBool("version"); showVersion {
        fmt.Println(version.Version)
        os.Exit(0)
    }
}
```

## 버전 관리 프로세스

### 1. 개발 단계

```bash
# 개발 버전으로 빌드
VERSION=1.6.0-dev ./build.sh

# 테스트
./git-ai-commit --version
# 출력: git-ai-commit version 1.6.0-dev
```

### 2. 릴리스 준비

1. `internal/version/version.go`의 버전 업데이트
2. CHANGELOG.md에 릴리스 노트 작성
3. 테스트 수행

### 3. 릴리스

```bash
# 릴리스 버전으로 빌드
VERSION=1.5.0 ./build.sh

# Git 태그 생성
git tag -a v1.5.0 -m "Release v1.5.0"

# Homebrew 업데이트 (선택)
```

## 향후 개선 가능성

1. **Git Commit Hash 포함**: 빌드된 커밋 해시 정보 추가
2. **자동 버전 업데이트**: Git 태그 기반 자동 버전 관리
3. **릴리스 자동화**: GitHub Actions 등 CI/CD 통합
4. **버전 확인 API**: 프로그래밍 방식의 버전 확인

## 관련 파일

- `internal/version/version.go`: 버전 정보 정의
- `cmd/root.go`: CLI 버전 플래그 구현
- `build.sh`: 빌드 스크립트

## 테스트

### 수동 테스트

```bash
# 1. 빌드
./build.sh

# 2. 버전 확인
./git-ai-commit --version

# 3. 기능 확인
./git-ai-commit --help
./git-ai-commit -detail high -lang ko
```

### 버전 주입 테스트

```bash
# 1. 커스텀 버전으로 빌드
VERSION=2.0.0-beta ./build.sh

# 2. 버전 확인
./git-ai-commit --version
# 출력: git-ai-commit version 2.0.0-beta (built 2026-02-11)
```

## 회고

### 잘한 점
- ✅ 버전 정보 중앙화로 관리 용이
- ✅ 빌드 스크립트 개선으로 자동화
- ✅ 사용자가 버전을 쉽게 확인 가능

### 개선할 점
- ⚠️ 자동 버전 관리 시스템 도입 필요
- ⚠️ 릴리스 프로세스 더 자동화 필요
- ⚠️ 버전 호환성 체크 기능 추가 가능

### 학습한 점
- 📚 Go Linker Flags 활용법
- 📚 버전 관리의 중요성
- 📚 빌드 시점 정보 주입 패턴

## 참고

- [Go Build Flags](https://golang.org/cmd/link/)
- [Cobra Documentation](https://github.com/spf13/cobra)
- [Semantic Versioning](https://semver.org/)